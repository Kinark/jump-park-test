import styled from 'styled-components'

const VerticalAligner = styled.main`
   height: 100vh;
   overflow: auto;
   justify-content: center;
   align-items: center;
   display: flex;
   flex-direction: column;
`

export default VerticalAligner
